#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  #+quicklisp(ql:quickload '(stumpwm) :silent t)
  )

(defpackage :ros.script.stumpwm.3759317089
  (:use :cl))
(in-package :ros.script.stumpwm.3759317089)

(defun main (&rest argv)
  (declare (ignorable argv))
  (stumpwm:stumpwm)
  )
;;; vim: set ft=lisp lisp:
